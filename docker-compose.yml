version: '3'
services:
  postgres:
    image: postgres:11
  nimble:
    build: utils
    depends_on:
      - postgres
    volumes:
      - .:/usr/src/app
    entrypoint: nimble
    working_dir: /usr/src/app
